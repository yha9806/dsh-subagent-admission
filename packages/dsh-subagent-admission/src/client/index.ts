/**
 * Client entry for the dsh-subagent-admission dual-face plugin.
 *
 * Task 1 ships the packaging skeleton only: this apply is a deliberate no-op.
 * The Snapshot Remote mount, locale, and read-only conversation view arrive in
 * later tasks.
 */

/** Stable plugin identity for the client module loader. */
export const name = 'dsh-subagent-admission'

/** Deliberate no-op until the client controller lands in a later task. */
export function apply(): void {}
